# Rust build artifacts
target/
**/target/
**/*.rs.bk
**/*.profraw
**/*.cov-bak

# Bridge release packaging artifact
/systemprompt-bridge.zip
docs
docs/
docs/*

# SQLx workspace cache (development only - per-crate caches are committed)
/.sqlx/

# Dependencies
node_modules/
**/node_modules/
package.json
package-lock.json

# TypeScript
*.ts
*.tsx
tsconfig.json
tsconfig.*.json
!bin/bridge/web/js/types/*.ts

# Database files
/database/*.db
/database/*.db-*
/database/*.sqlite
/database/*.sqlite-*
*.db
*.db-shm
*.db-wal

# Environment files
.env
.env.local
.env.*.local

# Secrets files
*.secrets.*
*secrets*.yml
*secrets*.yaml
*secrets*.json
*secrets*.toml

# Cargo config (may contain local paths)
.cargo/*
!.cargo/audit.toml

tests/validator

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code
.claude/

# Logs
*.log
/logs/
/log/

# Build output
/dist/
/build/

# Test artifacts
/tmp/
/temp/
/test-output/
mutants.out/
mutants.out.old/

# OS files
.DS_Store
Thumbs.db

# Audit files
*.audit
audit.md
audit.log
audit.json
audit/
.audit/
!crates/infra/security/src/authz/audit/
!crates/shared/models/src/audit/

# Service implementations (moved to implementation repos)
/crates/services/

/plan
/plan/*

# Internal documentation (local-only: dev guides, audits, evaluations, legal)
internal/

# Assets (local-only, not part of the published tree)
/assets/

# Local-only scripts
scripts/*
# ...except the committed shard runner, the single source of truth shared by CI
# (.github/workflows/ci.yml) and the local `just test-shard` recipes.
!scripts/test-shard.sh
# ...and the source-gate linters run by CI (.github/workflows/quality.yml) via
# the `just lint-*` recipes.
!scripts/lint-schema.sh
!scripts/lint-extensions.sh
!scripts/check-sqlx.sh
!scripts/check-http-errors.sh

# Local-only review notes
review/

# Scheduler unit tests (local-only)
crates/app/scheduler/tests/

# Coverage reports
coverage-report/

# Status files (local-only crate status docs)
**/status.md
*.cdx.json
systemprompt-sbom*
youtube-walkthrough.md

# Rust standards audit ledger (local-only, append-only)
/ISSUE.md
/.audit-state

# locally tracked dev tooling — not for crates.io
ci/
.sentrux/
Screenshot*.png
!crates/entry/api/src/services/gateway/audit/
!crates/entry/api/src/services/proxy/audit/
