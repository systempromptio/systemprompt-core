# RFI Readiness Audit — 2026-04-23

Snapshot of the codebase's RFI / enterprise-security review posture, prepared in response to the [REDACTED] inbound. Every item below reflects the state of `main` at the time of this audit; re-run the verification column to refresh.

## 1. Documentation Artefacts

| Artefact | Location | Status |
|----------|----------|--------|
| Vulnerability disclosure policy | `SECURITY.md` | ✅ published |
| Public evaluation pack entry point | `documentation/README.md` | ✅ published |
| STRIDE threat model | `documentation/security/threat-model.md` | ✅ published |
| Production deployment runbook | `documentation/security/deployment-reference-architecture.md` | ✅ published |
| Compliance control matrix (HIPAA / SOC 2 / ISO 27001) | `documentation/security/compliance-control-matrix.md` | ✅ published |
| Stability contract | `documentation/security/stability-contract.md` | ✅ published |
| Compatibility matrix (providers, protocols, runtime) | `documentation/security/compatibility-matrix.md` | ✅ published |
| Architecture (layered crates) | `instructions/information/architecture.md` (internal) + README.md (public) | ✅ published |
| Change history | `CHANGELOG.md` | ✅ active, per-release entries |
| Licence | `LICENSE` — BSL-1.1 with four-year conversion to Apache 2.0 | ✅ published |

## 2. Supply Chain

| Check | Tool | Status | Evidence |
|-------|------|--------|----------|
| RustSec advisory scan | `cargo deny check advisories` | ✅ clean (1 documented ignore) | `deny.toml` |
| Licence compliance | `cargo deny check licenses` | ✅ clean | `deny.toml` |
| Registry source lock | `cargo deny check sources` | ✅ clean (crates.io-only) | `deny.toml` |
| Duplicate / banned crates | `cargo deny check bans` | ✅ clean | `deny.toml` |
| GitHub Dependabot | GHSA feed | ✅ 1 LOW remaining (documented), down from 37 at start of audit | GitHub Security tab |
| SBOM (CycloneDX) | `cargo cyclonedx` | ✅ per-crate + facade, attached to release via `.github/workflows/sbom.yml` | CI |

### Documented Accepted Risk

**RUSTSEC-2023-0071 / GHSA-9c48-w39g-hm26 — Marvin Attack in `rsa` crate (LOW).**
Pulled transitively via `jsonwebtoken` for RSA-family JWT verification. No upstream fix available — the `rsa` crate does not yet offer constant-time implementations.
Mitigations in place:
1. Recommended signing algorithms for customer IdP tokens are ES256 / ES384 / EdDSA — these do not execute the vulnerable RSA path.
2. JWT verification is CPU-bounded and not an unauthenticated high-throughput endpoint, limiting exploitability of the timing side-channel.
3. Tracked — ignore is removed as soon as a fixed `rsa` release is available.

Full justification recorded inline in `deny.toml` under `[advisories].ignore`.

### Supply-chain Integrity on Release

- Release binaries are built in GitHub-hosted CI runners from tagged commits (`.github/workflows/release-sign.yml`).
- Signed with Sigstore `cosign` keyless, OIDC-bound to this repository and the release workflow.
- SHA256SUMS + per-file `.sig` + `.pem` bundle attached to every release.
- Verification command documented in `SECURITY.md`.

## 3. Continuous Integration

| Workflow | File | Triggers | Status |
|----------|------|----------|--------|
| CI (fmt, clippy, build, sqlx offline check) | `.github/workflows/ci.yml` | push on main, PR | ✅ green on `main` (commit 542cefd3) |
| Supply Chain (cargo-deny: advisories, licenses, bans, sources) | `.github/workflows/supply-chain.yml` | push, PR, daily cron | ✅ green on `main` (commit 542cefd3) |
| CodeQL (GitHub default security analysis) | GitHub-hosted | push, PR | ✅ green on `main` |
| SBOM (CycloneDX) | `.github/workflows/sbom.yml` | tag `v*`, manual | 🟡 untriggered (fires on next release tag) |
| Release + sign | `.github/workflows/release-sign.yml` | tag `v*`, manual | 🟡 untriggered (fires on next release tag) |

### Local Verification Performed

| Check | Command | Result |
|-------|---------|--------|
| Format | `cargo fmt --all -- --check` | ✅ clean |
| Lint | `cargo clippy --workspace --all-targets` | ✅ zero warnings |
| Offline build | `SQLX_OFFLINE=true cargo build --workspace --locked` | ✅ clean |
| Advisory + licence + bans + sources | `cargo deny check` | ✅ clean |
| SBOM generation | `cargo cyclonedx --format json` | ✅ produced (280-component facade SBOM) |

## 4. Code and Architecture

| Property | Evidence |
|----------|----------|
| Zero `unsafe` blocks outside crypto primitives | workspace-wide search |
| 100% SQL queries use compile-time verified macros (`sqlx::query!` / `query_as!` / `query_scalar!`) | `crates/infra/database/` + all repository crates |
| Typed identifiers (no raw `String` IDs) | `crates/shared/identifiers/` |
| Layer discipline enforced (shared → infra → domain → app → entry) | `crates/` layout; no circular deps in `cargo tree` |
| Memory-safe language | Rust edition 2024 |
| Single binary deployment | `bin/cowork` + `crates/entry/api` + `crates/entry/cli` |
| Postgres-only persistence | `crates/infra/database/` |
| At-rest secrets via ChaCha20-Poly1305 (authenticated encryption) | `crates/infra/security/` |
| OAuth2 with PKCE, JWT issuer + audience verification | `crates/infra/security/` + `crates/domain/oauth/` |
| Structured audit event pipeline (append-only) | `crates/infra/events/` |
| Extension framework (compile-time registered via `inventory`) | `crates/shared/extension/` |

## 5. Pre-answered Enterprise Security Questionnaire

Full pre-answers live in [compliance-control-matrix.md §4](compliance-control-matrix.md). Headline answers:

- **Certifications**: systemprompt.io as a vendor holds no SOC 2 / ISO 27001 / HITRUST certifications. The product is source-available infrastructure that the customer deploys inside their existing compliance boundary. Control-level support mappings provided.
- **BAA**: Not applicable — systemprompt as a vendor does not create, receive, maintain, or transmit PHI. Binary runs in the customer's environment.
- **Data location**: Customer's own Postgres, under customer control. The vendor never sees customer data.
- **Encryption**: TLS 1.2+ in transit (enforced at entry). Secrets at rest via ChaCha20-Poly1305. DB-level encryption at rest is customer-managed.
- **SSO**: OIDC through the customer's IdP.
- **SBOM**: CycloneDX, per release.
- **Release integrity**: cosign keyless, OIDC-bound to this repo, transparency-logged in Rekor.
- **Business continuity**: Source-available under BSL-1.1 with automatic conversion to Apache 2.0 after four years. Customer keeps indefinite usage rights under the licence.

## 6. Known Gaps (Honest List)

These are the artefacts an enterprise reviewer might ask for that are **not** yet in place. None are blocking for RFI response; all are addressable under a commercial engagement timeline.

| Gap | Why it matters | Plan |
|-----|----------------|-----|
| Third-party penetration test report | Large healthcare buyers frequently require one | Commission before first enterprise deployment or invite customer to run their own; offer to remediate findings |
| SOC 2 Type I / II attestation for systemprompt.io Ltd | Useful but not required for self-hosted model | Revisit when customer count + team size justifies audit cost |
| Cyber liability + E&O insurance certificate | Typical procurement checkbox | Quote and bind before contract signature |
| Formal incident-response playbook (beyond SECURITY.md) | Full IR runbook for customer-facing incidents | Draft alongside first paid customer |
| Public CI badges on README | Visible signal of maintained project | Add once workflows have produced a stable history |

## 7. Verification

To reproduce this audit from a fresh clone:

```bash
git clone https://github.com/systempromptio/systemprompt-core
cd systemprompt-core
cargo fmt --all -- --check
cargo clippy --workspace --all-targets
SQLX_OFFLINE=true cargo build --workspace --locked
cargo deny check
cargo cyclonedx --format json --describe crate
```

All should complete without errors on a clean checkout.

## 8. Revision

| Date | Change |
|------|--------|
| 2026-04-23 | Initial audit following [REDACTED] inbound. 37 Dependabot advisories resolved to 1 LOW documented ignore; public evaluation pack shipped; CI, Supply Chain, and CodeQL workflows all green on `main` (commit 542cefd3). |
