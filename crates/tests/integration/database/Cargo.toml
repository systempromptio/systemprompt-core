[package]
name = "systemprompt-database-integration-tests"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Integration tests for systemprompt-database lifecycle (schema/migration ordering)"
publish = false

[lints]
workspace = true

[dependencies]
systemprompt-database = { workspace = true }
systemprompt-extension = { workspace = true }
anyhow = { workspace = true }
sqlx = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
uuid = { workspace = true }

[package.metadata.cargo-machete]
# Used by test modules included via #[path="../*.rs"] outside src/, which cargo-machete's scan doesn't follow.
ignored = ["anyhow", "sqlx", "systemprompt-database", "systemprompt-extension"]
