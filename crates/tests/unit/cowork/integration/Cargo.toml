[package]
name = "systemprompt-cowork-integration-tests"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Unit tests for systemprompt-cowork integration layer (host apps, registry, registry parser)"
publish = false

[lints]
workspace = true

[dependencies]
systemprompt-cowork = { workspace = true }

[dev-dependencies]
