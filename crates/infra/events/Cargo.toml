[package]
name = "systemprompt-events"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Events module for systemprompt.io - event broadcasting and routing"

[lints]
workspace = true

[dependencies]
# Core runtime
tokio = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
async-trait = { workspace = true }

# Web framework
axum = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Utilities
once_cell = { workspace = true }

# systemprompt.io dependencies
systemprompt-models = { path = "../../shared/models", version = "0.0.12" }
systemprompt-identifiers = { path = "../../shared/identifiers", version = "0.0.12" }

[dev-dependencies]
