[package]
name = "systemprompt-template-provider"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Template provider traits and abstractions powering config-as-code — part of the systemprompt.io AI governance infrastructure."

[lints]
workspace = true

[features]
default = []
tokio = ["dep:tokio"]

[dependencies]
systemprompt-provider-contracts = { workspace = true }

async-trait = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = ["fs", "sync"], optional = true }

[dev-dependencies]
tokio = { workspace = true }
