-- Source-change provenance was declared but never written or read; the
-- table carried no owner scope and no foreign keys on its revision columns.
DROP TABLE IF EXISTS eval_campaign_source_changes;
