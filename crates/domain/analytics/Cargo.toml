[package]
name = "systemprompt-analytics"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Analytics module for systemprompt.io - session tracking, metrics, and reporting"

[lints]
workspace = true

[features]
default = []
web = ["dep:axum"]

[dependencies]
# Core runtime
tokio = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
async-trait = { workspace = true }

# Database
sqlx = { workspace = true }

# HTTP types (framework-agnostic)
http = { workspace = true }

# Web framework (optional, for Request extraction)
axum = { workspace = true, optional = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true }

# GeoIP
maxminddb = { workspace = true }
url = { workspace = true }

# systemprompt.io dependencies
systemprompt-database = { path = "../../infra/database" }
systemprompt-extension = { path = "../../shared/extension" }
systemprompt-identifiers = { path = "../../shared/identifiers", features = ["sqlx"] }
systemprompt-models = { path = "../../shared/models" }
systemprompt-traits = { path = "../../shared/traits" }

inventory = { workspace = true }
