[package]
name = "systemprompt-core-users"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Core user management module for SystemPrompt"

[lints]
workspace = true

[dependencies]
# Core dependencies
tokio = { workspace = true }
anyhow = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = { workspace = true }
chrono = { workspace = true }
inventory = { workspace = true }

# Database
sqlx = { workspace = true }

# HTTP Client for REST API calls
reqwest = { workspace = true }
urlencoding = { workspace = true }

# Core libs
systemprompt-models = { path = "../../shared/models" }
systemprompt-core-database = { path = "../../infra/database" }
systemprompt-core-logging = { path = "../../infra/logging" }
systemprompt-traits = { path = "../../shared/traits" }
systemprompt-provider-contracts = { path = "../../shared/provider-contracts" }
systemprompt-identifiers = { path = "../../shared/identifiers" }

# Traits
async-trait = { workspace = true }
thiserror = { workspace = true }

# Logging
tracing = { workspace = true }

# Security
uuid = { workspace = true }
rand = { workspace = true }

# Validation
validator = { workspace = true }

# Web interface dependencies (always included)
axum = { workspace = true }

[features]
default = ["web"]
web = []